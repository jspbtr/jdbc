package beer.app;

import beer.entities.BeerEntity;

public class BeerApplication {

	public static void main(String[] args) {

		BeerEntity be = new BeerEntity();
		// be.createBeer(101, "Budwiser", 140);
		// be.createBeer(102, "Tuborg", 135);
		// be.createBeer(103, "KingFisher", 130);

		// be.updateBeer(103, "Beera", 160);

		// be.retriveBeers();
		// be.retriveBeer(102);

		// be.deleteBeer(103);

		// be.closeConnectionInstance();

		// be.retriveBeers();

		// be.sortByBarcode();
	}

}
